<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.quote.ui.fragment.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_savedQuotesFragment"
            app:destination="@id/savedQuotesFragment" />
    </fragment>
    <fragment
        android:id="@+id/savedQuotesFragment"
        android:name="com.example.quote.ui.fragment.SavedQuotesFragment"
        android:label="fragment_saved_quotes"
        tools:layout="@layout/fragment_saved_quotes" />
</navigation>